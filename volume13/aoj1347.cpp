#include <bits/stdc++.h>
using namespace std;

typedef vector<int> vec;

int main () { 
    int N, m; cin >> N >> m;
    vec c(m), d(m);
    for (int i = 0; i < m; i++) {
        cin >> c[i] >> d[i];
    }

    while (que.size()) {

    }
    
    return 0;
}
