#include <bits/stdc++.h>
using namespace std;

int main () {
    int n, m;
    cin >> n >> m;

    vector<int> s(n), t(m);
    for (auto &i : s) cin >> i;
    for (auto &i : t) cin >> i;

    vector<int> r0, r1;
    for (int i = 0; i < m; i++) {
        for (int j = 0; j < t[i]; j++) {
            r0.push_bach()
                }
    }

    return 0;
}
