#include <bits/stdc++.h>
using namespace std;

typedef vector<int> vec;

int main () {
    int N, Q;
    while (cin >> N >> Q, N || Q) {
        vec p(N-1);
        for (auto &i : vec) cin >> i;

        char c;
        int node;
        
        for (int i = 0; i < Q; i++) {
            cin >> c >> node;

            if (c == 'M') {

            } else {

            }
        }
    }
    
    return 0;
}
