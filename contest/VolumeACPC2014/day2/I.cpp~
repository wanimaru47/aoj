#include <iostream>
#include <vector>
#include <algorithm>
using namespace std;

int n, q;

int main ()
{
    cin >> n;
    vector<int> a = (n, 0);
    for (int i = 0; i < n) {
        cin >> n[i];
    }
    int sp = sqrt(n), cl = 0;
    while (cl < n) {
        if (cl + sp < n)
            sort(v.begine() + cl, v.begine() + cl + sp);
        else
            sort(v.begine() + cl, v.end());
    }
    for (int i = 0; i < v.size(); i++) {
        cout << v[i] << " ";
    }
    cout << endl;
    
//    cin >> q;
    

    return 0;
}
