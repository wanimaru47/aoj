import std.stdio;
import std.string;
import std.conv;
import std.algorithm;

int main()
{
    while (true) {
        int n = readln.chomp.to!int;
        string[] str = readln().chomp.split;
        int[12] num;
        for (int i = 0; i < n; i++)
            num[i] = 
    }

    return 0;
}
