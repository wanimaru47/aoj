#include <iostream>
#include <cstring>
#include <queue>
#include <vector>
using namespace std;

int main()
{
    int N ;
    cin >> N;
    while (N--) {
        string str;
        cin >> str;
        int i = -0
        while (str[i] != '=') {
            int k = 0;
            while ('0' <= str[i] && str[i] <= '9') k++;
            string str;
            
        }
    }

    return 0;
}
