#include <iostream>
#include <string>
#include <algorithm>
#define MAX 30
using namespace std;

int main()
{
    int N;
    while (cin >> N) {
        int date[N][MAX];
        int dp[N][MAX];
        memset(dp, 0, sizeof(dp));
        for  (int i = 0; i < N; i++) {
            int n;
            cin >> n;
            date[i][0] = n
                for (int f = 1; f <= n; f++) 
                    cin >> date[i][f];
        }
        for (int i = 0; i < MAX; i++)
            for (int j = 0; j < N; j++)
                
            
    }

    return 0;
}
