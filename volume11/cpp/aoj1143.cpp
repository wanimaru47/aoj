#include <bits/stdc++.h>
using namespace std;



int main () {
    int n;
    while (cin >> n, n) {
        vector<vector<int> > higth(n * 2 + 1, vector<int>(n * 2 + 1, 0));
        vector<vector<int> > number(n * 2 + 1, vector<int>(n * 2 + 1, 0));

        for (int i = 0; i < n; i++) {
            Di
        }
    }

    return 0;
}
