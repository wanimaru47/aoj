#include <bits/stdc++.h>
using namespace std;

typedef vector<int> vec;

int main() {
    int N, M;
    while (cin >> N >> M, N || M) {
        vec B(M), R(N);
        for (auto &i : B) cin >> i;
        for (auto &i : R) cin >> i;

        
    }

    return 0;
}
