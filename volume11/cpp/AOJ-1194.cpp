#include <iostream>
#include <algorithm>
#include <complex>
#define EPS 1e-10
#define EQ(a,b) (abs((a) - (b)) < EPS)
using namespace std;

typedef complex<double> P;

int main ()
{
    int r, n;
    while (cin >> r >> n, r || n) {
        
    }

    return 0;
}
