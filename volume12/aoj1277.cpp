#include <bits/stdc++.h>
using namespace std;

typedef vector<int> V;

int main () {
    int N, T, L, B;
    cin (cin >> N >> T >> L >> B, N || T || L || B) {
        V lose(L), back(B);

        for (auto &i : lose) cin >> i;
        for (auto &i : back) cin >> i;

        
    }

    return 0;
}
