#include <bits/stdc++.h>
using namespace std;

int main() {
    double d;
    cin >> d;

    if((int)d == d) {
        printf("%.9f\n", d + 1.);
    } else {
        double l = 0., r = d * d;
        while() {
            double mid = (l + r) / 2.;
        }
    }

    return 0;
}
